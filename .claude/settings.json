{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.py) out=$(ruff check \"$f\" 2>&1); code=$?; [ \"$code\" -ne 0 ] && jq -n --arg out \"$out\" '{hookSpecificOutput:{hookEventName:\"PostToolUse\",additionalContext:$out}}'; true;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
